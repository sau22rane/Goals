<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sau22rane.goals">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".EditGoalsPopup"
            android:theme="@style/AppTheme.CustomTheme"/>

        <activity android:name=".AddGoalsPopup"
            android:theme="@style/AppTheme.CustomTheme"/>

        <activity android:name=".Goals">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".EditRitualsPopup"
            android:theme="@style/AppTheme.CustomTheme" />
        <activity
            android:name=".AddRitualsPopup"
            android:theme="@style/AppTheme.CustomTheme" />
        <activity
            android:name=".EditWorkDonePopup"
            android:theme="@style/AppTheme.CustomTheme" />
        <activity
            android:name=".AddWorkDonePopup"
            android:theme="@style/AppTheme.CustomTheme" />
        <activity
            android:name=".EditExpectationsPopup"
            android:theme="@style/AppTheme.CustomTheme" />
        <activity
            android:name=".AddExpectationsPopup"
            android:theme="@style/AppTheme.CustomTheme" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">

        </activity>
    </application>

</manifest>